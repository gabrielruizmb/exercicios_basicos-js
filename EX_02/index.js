function calcularÁreaRetângulo() {
  let base = Number(window.prompt('Insira o tamanho da base do retângulo: '));
  let altura = Number(window.prompt('Insira a altura do retângulo: '));
  window.alert(`A área do retângulo é igual a: ${base * altura}`);
}

calcularÁreaRetângulo();

